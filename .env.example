# Nano Banana API Configuration
# Copy this file to .env.local and add your actual API key
# The API key will be stored in browser's localStorage when entered in the UI

# Note: This is a client-side application, so the API key is stored in localStorage
# For production deployments, consider implementing a backend proxy to handle API keys securely
VITE_APP_NAME=Pixel Engine
